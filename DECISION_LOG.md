# Project Centaur Decision Log

Read this file first, then consult the canonical detailed log in:
- `docs/DECISION_LOG.md`

## Current High-Signal Decisions
- Shadow evidence provenance on 2026-05-31: shadow proposal and outcome rows now carry `environment`, `mode`, `source_environment`, `data_provider`, and `execution_provider`, and pending outcome checkpoints inherit metadata from their parent proposal. This supports paper/live evidence separation without changing strategy behavior or trade gates.
- Runtime live-boundary guard on 2026-05-31: explicit `CENTAUR_MODE=paper` now skips live broker sync and live order mutation paths, while `live_dry` may carry intended live approvals but `execution_live` will not submit broker orders. The current approved live follower remains active because legacy activation resolves runtime mode to `live`. This is a safety boundary; no risk limits were widened.
- Runtime mode label honesty on 2026-05-31: `CENTAUR_MODE`/`CENTAUR_ENVIRONMENT` remain explicit when configured, but legacy `.env` files that predate those fields now report `live/live` when the existing approved live activation flags are already armed. This changes status/provenance labeling only and does not alter live gates or order behavior.
- Broker order ledger provenance on 2026-05-31: `paper_trade_orders` now records explicit `environment`, `mode`, `source_environment`, `data_provider`, and `execution_provider` alongside `broker_id`, and existing `alpaca_live` rows are backfilled as live rows. This makes the shared broker-order ledger auditable while paper/live persistence is being separated. No order routing, live gates, notional, or trading policy changed.
- Runtime context, execution router, live guard, and instrument registry foundation on 2026-05-31: Centaur now has a `ModeContext` that centralizes mode/environment permission checks, treats `live_dry` as live-environment but non-mutating, and drives the live broker boundary checks. It also has an `ExecutionRouter` plus `LiveRiskGuard` for approved entry orders, managed exits, and stale-order cancellations, live account/sync/capacity/latest-bar and instrument/venue checks where derivable, same-paper-order validation where relevant, a canonical instrument registry with venue-specific mappings such as `ALPACA:BTC/USD` and `BINANCE:BTCUSDT`, a first-class `InstrumentRef` resolved identity object, instrument metadata on broker/shadow evidence rows with derivable existing rows backfilled, Alpaca market-data adapter payloads plus new latest-bar / historical-bar / strategy-candidate-signal rows plus discovery candidates and strategy signals carrying serialized `instrument_ref` metadata without a full historical control-loop sweep, an initial market-data adapter wrapper for current Alpaca latest-bar and historical backfill fetches, and an initial execution-adapter boundary used for order-request building and order mutation while still bridging to current broker adapters. This is architecture scaffolding and guard tightening only; no risk policy was widened, and no notional or broker routing changed.
- Storage-separation evidence surface on 2026-05-31: `.venv-mac/bin/python main.py --storage-separation-report` now samples recent broker-order, shadow-proposal, and strategy-fitness rows to verify paper/live/evidence provenance and state the current physical split status. Current implementation is PostgreSQL row-level provenance; physical paper/live database or schema separation remains pending.
- Live same-as-paper clarification on 2026-05-31: the operator did not request `max_daily_orders` or separate live-only threshold policy. Alpaca Live should be exactly the paper lane, but live. The live guard remains only as a final real-money safety boundary for activation, kill switch, account/sync readiness, broker identity, and same-paper-order validation.
- PostgreSQL schema separation scaffold on 2026-05-31: `POSTGRES_SCHEMA` is now honored by `UsageLedger`; when configured, the operations store creates/uses that schema and reports it in backend detail. Unset keeps current behavior. This is storage scaffolding only; active paper/live lane migration still requires a separate checklist.
- Core/paper/live storage layout on 2026-05-31: Centaur now models storage as three lanes: shared `core` reviewed evidence/fitness/instruments, `paper` execution/evidence, and `live` execution/evidence. The storage report prints those lane schemas and directories so paper/live can separate without duplicating the prediction brain.
- Root storage cleanup on 2026-05-31: the empty `storage/` placeholder tree was removed because it contained only `.gitkeep` files and active operation is PostgreSQL-backed. Lane paths remain in config/docs, and `StorageLayout.ensure_directories()` can recreate file-output folders lazily when needed. This does not change scheduler behavior or persistence.
- Storage lane bootstrap on 2026-05-31: `scripts/bootstrap_storage_lanes.py` initializes the configured PostgreSQL `core`, `paper`, and `live` schemas/tables without changing the active scheduler schema. This is readiness work only; unattended lane cutover remains separate.
- `CENTAUR_CONFIG` default layer on 2026-05-31: runtime config now reads the paper/live YAML file named by `CENTAUR_CONFIG` as defaults, with `.env` still taking precedence. This makes the core/paper/live deployment examples operational scaffolding rather than dead files.
- Adapter inventory surface on 2026-05-31: `.venv-mac/bin/python main.py --adapter-inventory` lists active Alpaca adapters, bridged Alpaca paper/live execution, scaffold-only IG account support, and not-implemented future providers such as Binance, Coinbase, and Polygon. This is observability only and does not make any non-Alpaca provider tradable.
- Execution adapter allowlist on 2026-05-31: `centaur.execution_adapters` resolves only `alpaca_paper` and `alpaca_live`; IG, Binance, Coinbase, and other future providers fail at adapter lookup until concrete execution adapters, tests, evidence surfaces, and explicit approval exist.
- Physical architecture tree on 2026-05-31: the target `app/` package now exists as facade modules for `core`, `engine`, `adapters`, `runtime`, `storage`, `reporting`, and `strategies`, while behavior remains in the current `centaur/` implementation. This creates the folder/import boundary without changing scheduler behavior, broker routing, live policy, or risk gates.
- First app-owned implementation slice on 2026-05-31: runtime mode context, live guard, execution router, execution adapters, market-data adapters, canonical instruments, and storage layout now have implementation ownership under `app/`. The corresponding `centaur/` modules remain as compatibility wrappers. Active pipeline imports were switched to the `app/` boundaries for these areas. This is an architecture migration only; live remains same-as-paper and no broker routing, notional, strategy policy, or risk gate changed.
- Live-dry order-action intents on 2026-05-31: `ExecutionRouter` now records intended live-dry entry/exit/cancel actions in tick state and persists router intents in `execution_router_intents` for review through `--evidence-report`; live-dry stale reaping / managed exits can produce intended action records without calling the live broker adapter. This is dry-run auditability only; no real order behavior or risk policy changed.
- Strategy fitness evidence-origin metadata on 2026-05-31: strategy fitness snapshots now persist `environment`, `mode`, `source_environment`, `broker_id`, `data_provider`, and `execution_provider`. Live may read paper/shadow/backtest-derived fitness as labeled evidence, but those rows are not live outcomes and do not grant live permission by themselves. This is persistence/auditability only; no trading policy changed.
- Adapter-first Centaur architecture recorded on 2026-05-31: `centaur-codex-architecture-instructions.md` is now part of the read-first stack. Future refactors should treat Alpaca as the first adapter, not the product; use explicit runtime modes; separate paper/live config, persistence, evidence, and permissions; and preserve PostgreSQL-only scheduler-backed paper/live operation. This is architecture guidance only; no trading policy changed.
- $50/day strategic target recorded on 2026-05-31: Project Centaur should build toward a sustained, evidence-backed `$50/day` net profit pace by improving throughput, expectancy, exit/data reliability, and staged slot/capital scaling. The plan is documented in `docs/FIFTY_DOLLAR_DAY_PLAN.md` and downloadable from the root page. This is a prioritization goal only; no trading policy changed.
- Slot-compounding honesty on 2026-05-31: the root slot-compounding page now defaults from closed Alpaca Paper round-trip metrics in `var/dashboard_snapshot.json`: observed win rate, average winning trade percent, average losing trade percent, observed slot-throughput pace, and account-level tracked P/L. This is read-only projection cleanup only; it does not affect execution, notional, broker routing, slot policy, or risk gates.
- Dashboard snapshot refresh on 2026-05-31: keep `CONTROL_REFRESH_DASHBOARD_SNAPSHOT=false` so normal 30-second trading ticks do not wait on dashboard/report work, but add a separate `launchd` lane `com.ghostfrog.centaur.dashboard-snapshot` that refreshes `var/dashboard_snapshot.json` every `300` seconds with skip-if-busy locking. This changes operator-data freshness only; no trading policy, broker routing, notional, live-follow rule, or risk gate changed.
- Status/dashboard honesty pass on 2026-05-31: operator surfaces now label paper account values, live follower/read-only monitoring, broker-order ledger rows, latest-tick activity, recent samples, all-time evidence, shadow-only recommendations, and observe-only drawdown evidence more explicitly. This is presentation/auditability cleanup only; no trading policy changed.
- Database query/index audit on 2026-05-31: regular status, dashboard, evidence, strategy-health, paper-exit, and holding-window paths now have narrow supporting indexes for tick usage rollups, FX reference lookups, latest-bar windows, candidate-signal counts, paper-order activity, proposal/order joins, and evaluated shadow outcomes. PostgreSQL schema bootstrap now serializes concurrent status/report starts with an advisory lock. This is a performance/auditability cleanup only; no trading policy changed.
- Status/database cleanup on 2026-05-31: `main.py --status` now uses a lightweight threshold-advice sample instead of full GA work, and dashboard-only visual datasets stay out of the CLI heartbeat path. Operations schema now adds timestamp/high-water indexes for recent ticks, broker account snapshots, and shadow proposal reporting.
- Tighten operations-store enforcement on 2026-05-31: when PostgreSQL is configured, or paper/live execution is enabled, `UsageLedger` must fail closed if PostgreSQL is unavailable rather than silently falling back to SQLite. Explicit SQLite remains a local/dev-only path when execution is not enabled.
- Core directive added on 2026-05-31: any new feature that captures learning/evidence data must be wired into a runnable report or evidence surface, documented with interpretation guidance, and designed with database/index discipline so reports and control ticks do not create avoidable drag.
- Add an observe-only trailing drawdown/high-water feature on 2026-05-31: Centaur records paper/live broker equity peaks for the current session, current giveback from that peak, and whether a configured future guard would have blocked new entries. This is data collection only; it does not affect CFO gates, managed exits, live following, orders, or protection latches without a separate explicit override. Broad report requests should start with `.venv-mac/bin/python main.py --evidence-report`, which lists all active shadow/counterfactual evidence streams before deciding actions.
- By explicit human override on 2026-05-31 after the operator objected to a `LINK/USD` loss sold only by `max_holding_window_elapsed`, managed max-hold exits for `profit_after_1h_else_1d` and `profit_capture_else_1d` now defer red positions with `max_hold_red_deferred`; stops, profit capture, take profit, and Friday equity no-weekend-carry flattening remain active.
- By explicit human override on 2026-05-31, add an equity no-weekend-carry guard: keep most Friday equity trading available, block new equity entries during the final `60` minutes of the regular Friday session, and flatten managed equity positions during the final `15` minutes with exit reason `friday_no_weekend_carry`; this reduces weekend gap and Monday forced-exit timing risk without disabling crypto weekend trading.
- GO-LIVE OVERRIDE: on 2026-05-29 at about 10:48 BST, the operator explicitly instructed Codex to turn on Alpaca Live. Final read-only checks immediately before activation showed Alpaca Live `ACTIVE`, not blocked or suspended, `132.05` cash/equity/buying power, `0` positions, `0` recent orders, `0` open orders, live daily protection active at baseline `132.05`, and current guard still blocked by `live_execution_disabled`. The authorized activation is limited to the recorded same-as-paper follower lane only.
- First live-enabled observation: tick `20260529-105313` completed `OK`; live CFO held with `no_paper_approved_trade_to_follow`, live execution submitted `0` orders, and post-activation read-only checks still showed `0` live positions, `0` recent live orders, and `0` open live orders.
- Record the first-live activation plan on 2026-05-29: operator-selected policy is a same-as-paper Alpaca Live follower lane using the current paper strategies, equities plus crypto, `$10` entries, `10` base slots, one order per tick, `$5.00` daily protector, shared paper/shadow fitness, and read-only live execution intelligence. The account shows `132.05`, but only `$100` is launch operating bankroll; the extra `32.05` is buffer.
- Alpaca Live funded read-only checks passed on 2026-05-29: account `ACTIVE`, not blocked/suspended, `132.05` cash/equity/buying power, `0` positions, `0` recent orders, and `0` open orders. Live entries remain blocked by `live_execution_disabled`; the `$132.05` balance does not widen the intended `$10 x 10` live envelope.
- Add a read-only live execution intelligence lane: strategy intelligence remains shared shadow-fitness intelligence, while live execution monitoring separately compares future `alpaca_live` entry orders against same-proposal paper entries for fill drift, status mismatches, unmatched live orders, and current live blockers. This adds visibility only and does not create an independent live strategy scorer or alter order submission gates.
- By operator request on 2026-05-29, prepare the dormant Alpaca Live lane so its readiness envelope mirrors paper: equities and crypto, the same three allowed strategies, `$10` notional, `10` base slots, one order per tick, `$5.00` daily drawdown protector, `1.5%` equity projected-gain floor, `2.0%` crypto projected-gain floor, `5` bps equity limit buffer, and `25` bps crypto limit buffer. This is readiness only; `LIVE_EXECUTION_ENABLED=false`, `LIVE_EXECUTION_KILL_SWITCH=true`, empty live credentials, and empty `LIVE_EXECUTION_ACTIVATION_ACK` still block real-money order submission until a separate go-live override records rollback rules.
- POTENTIAL BREAKTHROUGH: the Centaur paper lane is a micro-trading system, so its edge must be judged as repeated `$10` entries with small exits, not as tiny positions waiting for big-trade percentage targets. On 2026-05-28, aligning high-score near-miss entries with a `1.25%` profit capture immediately restarted trading and produced visible small green P/L; treat this as a hypothesis to validate with repeated outcomes, not a license to widen risk.
- FIRST PROFITABILITY CHECK: when Centaur is not compounding, check whether exit targets or holding windows are too ambitious before changing entry logic. The 2026-05-28 crypto and snapback review showed that small meaningful wins can appear before the old larger targets, then fade; exit design must be diagnosed first.
- Record profit-target ladder counterfactuals in shadow outcomes via `SHADOW_PROFIT_TARGET_LADDER_PCT=1.25,2,3,4,6`, so paper can keep selling at `1.25%` while the review trail shows whether higher targets would have hit later.
- By explicit human override on 2026-05-28, add a paper-only high-score near-miss override: already allowed paper strategies with raw `signal_score >= 90.0` can survive fitness suppression when their composite fitness is within `0.25` of the active threshold, while keeping `$10` notional, stops, projected-gain floors, broker routing, max orders, the strategy allowlist, and live execution unchanged.
- By explicit human override on 2026-05-28, change `crypto_momentum.trend` paper managed exits from a `60m` time exit to `profit_capture_else_1d`, keeping stop loss, target, `1.25%` profit capture, `$10` notional, broker routing, and live execution unchanged.
- By explicit human override on 2026-05-28, add a paper managed-exit profit capture at `1.25%` for both equity and crypto positions, while preserving the existing entry projected-gain floors, stops, broker routing, notional, slot caps, and live safe-off lane.
- By explicit human override on 2026-05-28, split the paper limit buffer by asset class and set crypto to `25` bps while equities remain at `5` bps, so Alpaca Paper crypto IOC limits are more likely to fill without changing notional, broker routing, strategy allowlist, projected-gain floors, slot caps, or live execution.
- Normalize Alpaca crypto symbols in paper managed exits so slashless position symbols such as `AAVEUSD` match stored entry plans and bars such as `AAVE/USD`; this preserves existing stop/target/time exits without changing entry policy.
- By explicit human override on 2026-05-28, lower the crypto-only discovery floor from `CRYPTO_MOMENTUM_MIN_DISCOVERY_SCORE=4.5` to `2.5` so `crypto_momentum.trend` can turn more overnight crypto candidates into actual strategy signals; this does not change the equity lane, notional, suppress thresholds, or projected-gain rules.
- By explicit human override on 2026-05-27, tighten the unattended `launchd` cadence from `60` seconds to `30` seconds and require wrapper-level busy-skip locking, so Centaur can probe more often without overlapping control ticks.
- By explicit human override on 2026-05-27, disable automatic dashboard snapshot refresh after each control tick in the current headless trading mode. `write_dashboard_snapshot()` remains available as a manual/separate operator action, so the trading loop is no longer forced to wait for `StatusReporter().snapshot()` before exiting.
- By explicit human override on 2026-05-27, split the suppress threshold by asset class and then loosen the crypto lane: equities keep the existing adaptive suppress-threshold lane anchored at `-5.60`, while crypto now uses its own fixed suppress threshold `STRATEGY_ALLOCATION_CRYPTO_SUPPRESS_THRESHOLD=-6.90` so the current `crypto_momentum.trend` fitness area near `-6.85` can survive suppression.
- Add a read-only `--crypto-health` operator command that summarizes recent `crypto_only_window` ticks, crypto bar-fetch activity, raw-vs-suppressed crypto signal visibility, selected overnight crypto candidate symbols, and the latest crypto fitness snapshot, so overnight crypto questions can be answered from one deterministic workflow.
- By explicit human override on 2026-05-27, widen the crypto market-data and discovery universe from `5` to `11` USD pairs by adding `LTC/USD`, `BCH/USD`, `LINK/USD`, `AVAX/USD`, `UNI/USD`, and `AAVE/USD`, while keeping the shared `$10` notional, one-order-per-tick cap, broker routing, and stricter crypto-specific entry knobs unchanged.
- By explicit human override on 2026-05-27, split `crypto_momentum.trend` onto its own conservative runtime knobs: stop loss `3.0%`, target multiple `2.0`, min signal score `60`, min movement `0.15%`, min discovery score `4.5`, min trade count `2`, and a crypto-specific paper projected-gain floor of `2.0%`, while keeping the shared `$10` notional, slot caps, broker routing, and suppress-threshold rails unchanged.
- Add a read-only `--strategy-health` operator command that bundles actual paper P/L, recent exit damage, proposal flow, raw-versus-suppressed strategy activity, and the existing exit/holding advisories into one report, so repeated trade-diagnosis questions can be answered from one deterministic workflow.
- By explicit human override on 2026-05-27, tighten the paper-only threshold rails after the recent red drift: raise the fixed suppress threshold from `-5.70` to `-5.60`, raise the adaptive floor from `-6.50` to `-6.40`, and restore the cliff-governor safety gap from `0.05` to `0.10` so Centaur keeps trading but stops admitting the recent weak `mean_reversion.snapback` band around `-6.77`.
- Add a read-only `7d` shadow checkpoint to the default shadow-outcome trail and surface it in the holding-window and paper-exit review outputs, so operators can answer "what if we had held longer?" without changing live paper exits.
- Refresh stale or non-marketable paper sell exits before submitting a replacement, so an old open exit order cannot strand a losing managed position behind `exit_order_already_open`.
- By explicit human override on 2026-05-26, replace the blunt `1h` timed paper exit for `mean_reversion.snapback` with `profit_after_1h_else_1d`: keep the existing stop/target protections, allow profitable positions to exit after one hour, and otherwise hold until the one-day backstop.
- Add a read-only paper-exit post-mortem command that compares actual paper exits with stored `15m`/`1h`/`1d` shadow outcomes for the same proposal ids, so exit-quality review becomes a repeatable operator workflow instead of ad hoc database forensics.
- Surface Centaur's activity trail in status/dashboard views, including raw strategy signals, fitness-suppressed signals, surviving signals, and the current trade blocker, so "no Alpaca order" is not mistaken for "system idle."
- Add a guarded paper-only adaptive controller for the strategy-allocation suppress threshold; it uses the trade-aware GA adviser, a current-tick cliff governor, and persisted state to follow clean paper-allowed cliffs while keeping blocked/disallowed cliffs separated, and must not mutate `.env` or any other paper/live risk policy.
- Add a recommendation-only holding-window fitness adviser for `mean_reversion.snapback`; it compares fixed `15m`/`1h`/`1d` outcomes and simple dynamic policies, but must not alter managed paper exits without a separate explicit override.
- Preserve Centaur's managed-exit plan across Alpaca order refreshes and recover missing stop/target/holding-window data from the matching shadow proposal, so broker payload polling cannot silently strand an aged paper position.
- Start the multi-broker refactor by putting the working Alpaca execution path behind a broker adapter first, so current paper trading stays stable while IG is scaffolded.
- Keep IG scaffold-only for now; do not activate it for execution until its minimum-bet and leverage math can pass the `$10` / `1x` safety rules.
- Persist `broker_id` on paper-trade orders so future multi-broker reporting does not pollute Alpaca and IG execution history together.
- Use Gemini API as the current LLM layer.
- Keep the Gemini adapter in the repo, but allow Gemini analysis to be disabled by config so Centaur can run in function-only mode when API cost is too high.
- Temporarily hide the standalone `Graphs` dashboard tab while diagnosing Tk UI responsiveness; keep the rest of the dashboard active so monitoring remains available.
- Temporarily hide all dashboard tabs and run the Tk monitor in a single flat view while diagnosing UI responsiveness.
- Temporarily disable the live runtime-log and wrapper-log tail panes in the Tk monitor while diagnosing UI responsiveness.
- Temporarily disable the recent-activity panel in the Tk monitor while diagnosing UI responsiveness.
- Make the Tk dashboard use a lighter status snapshot and reuse it for rendering, instead of building a second full snapshot on every refresh.
- Build the system as explicit pipelines, not a monolithic agent.
- Use PostgreSQL as the live operations store.
- Prefer `launchd` over cron on this Mac for unattended scheduling.
- On the migrated Mac, make the `launchd` wrapper prefer the project-local `.venv-mac` Python environment so unattended ticks use a modern interpreter with the PostgreSQL driver installed.
- Keep unattended runtime logs in the home directory, not on the external project volume.
- Start with shadow trading before broader paper execution.
- Keep Gemini out of the critical path for shadow proposal generation.
- Use deterministic, pluggable strategy profiles.
- Use historical replay to accelerate training.
- Use simple managed paper orders instead of fractional bracket entries.
- Surface broker failures directly in status and dashboard views.
- Score shadow and replay outcomes with configurable execution friction instead of perfect-fill assumptions.
- Show open-position and trade-blocker diagnostics in status and dashboard views.
- Show API cost visibility in the dashboard and explicitly flag when pricing inputs are incomplete.
- Pin Gemini cost accounting to the official `gemini-2.5-flash` rates instead of leaving pricing at zero.
- Support a one-shot historical API cost repricing pass so older usage rows can be recalculated from stored units when pricing changes.
- Keep `momentum.volatility_breakout` in shadow mode until it earns enough evidence.
- Separate recent-window strategy counts from all-time training volume in the dashboard.
- Rank dashboard strategy rows by current best fitness and explain why the top strategy is top, while still surfacing sample-size caveats.
- Surface Alpaca account balance and day-change information directly in status and dashboard views.
- Give account information its own dashboard tab and a header day-P/L badge for faster at-a-glance reading.
- Show the header day-P/L in both USD and GBP so the operator can read paper-account movement in local currency without leaving the dashboard.
- Surface the paper capital envelope directly in the dashboard and status view so the `$10 x 10 = $100` operating bankroll is visible, not just implied.
- Compare Centaur’s current paper-trading pace against simple long-term investing yardsticks on the same bankroll, while clearly labeling that short paper samples are not proof.
- Anchor that return-comparison view to the last persisted tick before the first paper order, not the mutable paper-order row's current `tick_id`, so later order polling does not rewrite the starting baseline.
- Keep written strategy-selection and go-live checklists in the repo and expose them as dashboard-readable tabs.
- Widen the paper envelope only by explicit human override; the current override allows up to `10` paper positions, enables crypto paper trading, and swaps equity discovery to the full Nasdaq-100 universe while keeping `$10` notional and one-order-per-tick discipline.
- Harden micro-trade economics with a fixed shadow/replay friction floor, a minimum projected-gain gate, and marketable limit paper orders instead of raw market orders.
- Add a persisted daily equity-drawdown protector and a stale-entry-order reaper inside the main control pipeline.
- Persist broker account snapshots by `broker_id` and show broker-separated account state in status/dashboard surfaces before any IG execution is activated.
- Preserve broker-reported fractional exit quantities and only round down to Alpaca's 9-decimal fractional precision, because rounding managed-exit quantities upward causes insufficient-quantity rejects.
- Enable `momentum.volatility_breakout` for micro paper execution by explicit human override on 2026-04-15, while preserving `$10` notional, Alpaca Paper routing, the one-order-per-tick cap, and the `$5.00` daily protector.
- Lower the strategy-allocation suppress threshold from `-5.0` to `-5.25` by explicit human override on 2026-04-21 after a seven-day flatline, while preserving `$10` notional, Alpaca Paper routing, allowed strategies, market-hours, projected-gain, max-slot, and daily-protection rules.
- Lower the strategy-allocation suppress threshold again from `-5.25` to `-5.50` by explicit human override on 2026-05-07 after a second flatline, while preserving `$10` notional, Alpaca Paper routing, allowed strategies, market-hours, projected-gain, max-slot, and daily-protection rules.
- Lower the strategy-allocation suppress threshold again from `-5.50` to `-5.70` by explicit human override on 2026-05-11 after current `mean_reversion.snapback` signals were suppressed around `-5.669`, while preserving `$10` notional, Alpaca Paper routing, allowed strategies, market-hours, projected-gain, max-slot, and daily-protection rules.
- Persist a compact per-tick blocker summary in the state snapshot and surface it in status so entry-stage and exit-stage blockers can be audited directly from each stored tick.
- Make the primary dashboard path a DDEV/OrbStack-routed web app backed by a host-written `StatusReporter` snapshot file, and keep the direct Python web view plus the older Tk monitor only as fallback surfaces.
- Prepare an Alpaca Live sidecar lane for a possible May 2026 go-live discussion; it can only follow same-tick paper-approved trades that paper execution actually submitted, and remains disabled unless credentials, activation acknowledgement, live allowlist, kill-switch, and daily-loss gates are explicitly set.
- Harden the dormant Alpaca Live lane so its protective mechanics match paper readiness: live account readiness, submitted-paper-order follow rule, persisted live daily protection, guarded cancellation, stale-entry reaping, and stale/non-marketable managed sell-exit refresh, while keeping live entries disabled by default.
- Require docstrings or compact nearby comments on safety-critical trading paths so future changes preserve the intended risk gate, capital-protection reason, and audit trail.
- Add an earned-slot compounding rule for paper and future live: each full `$10` of tracked P/L above the account lane's pre-first-order baseline adds one effective `$10` slot, while the per-trade notional remains fixed at `$10`.

## Canonical File
The detailed rationale, chronology, and implementation notes live in:
- `docs/DECISION_LOG.md`
